# PRD: 메인 피드 UI 및 통합 필터링 시스템 (Main Feed UI)

## 1. 개요
- **목표:** 전문가로부터 수집 및 분석된 경제 인사이트를 사용자가 한눈에 확인하고, 테마/계정/기간별로 정밀하게 필터링하여 탐색할 수 있는 고도화된 메인 화면을 구축한다.
- **주요 작업:** 다중 선택 필터 시스템 구현, 인사이트 카드 UI 개발, 전역 다크모드 적용, 실시간 데이터 연동.

## 2. 사용자 스토리
- "사용자는 여러 개의 경제 테마와 여러 명의 분석 계정을 동시에 선택하여 맞춤형 피드를 구성할 수 있어야 한다."
- "사용자는 기간 선택기를 통해 과거의 특정 시점의 경제 인사이트를 조회할 수 있어야 한다."
- "사용자는 선택된 필터의 상태(예: 선택된 계정 수)를 항상 명확하게 인지할 수 있어야 한다."
- "사용자는 검색어를 통해 요약 내용이나 원본 트윗 본문 내의 키워드를 빠르게 찾을 수 있어야 한다."

## 3. 기술적 상세 및 요구사항

### 3.1 필터링 및 데이터 로직
- **통합 조회 API:** `expertIds`, `categories`, `dateRange`, `searchQuery`를 동시에 처리하는 Supabase 조인 쿼리 구현.
- **상태 관리 규칙:** 
    - `undefined`: 초기 상태 (해당 필터 무시, 전체 조회).
    - `[]` (빈 배열): 명시적 선택 해제 (결과 없음 처리).
- **실시간 반영:** TanStack Query를 활용하여 필터 변경 시 즉시 데이터를 리프레시.

### 3.2 디자인 및 UX 사양
- **테마:** 시스템 설정과 무관하게 **다크모드(Dark Mode) 고정** 적용.
- **인사이트 카드:**
    - 중요도(`High`, `Medium`, `Low`)에 따른 배지 색상 차별화.
    - 전문가 아이디(@handle) 강조 및 원본 트윗 연결 링크 제공.
    - 작성 시간의 상대적 표기 (예: "3시간 전").
- **필터 UI:** 
    - 분석 계정 목록에 `ScrollArea` 적용 (최대 높이 300px).
    - 선택 상태를 하단에 상시 노출 ("xx개 계정 선택됨").
    - 필터 초기화 버튼을 통한 원클릭 리셋 기능.

## 4. 단계별 구현 로드맵

### 1단계: API 및 데이터 조회 로직 (Entities)
- [x] `src/entities/insight/api/insight.ts` 작성 (다중 필터 지원 통합 쿼리).

### 2단계: 필터 및 카드 컴포넌트 개발 (Widgets/Features)
- [x] `InsightCard`: 요약 및 전문가 정보 시각화 완료.
- [x] `ExpertMultiSelect`: 전체 토글 및 상시 상태 노출 로직 구현 완료.
- [x] `DateRangePicker`: 기간 선택 UI 구현 완료.
- [x] `FeedFilter`: 검색어, 카테고리, 전문가, 기간 통합 필터바 완성.

### 3단계: 메인 페이지 구성 (Views/App)
- [x] `MainFeedView`: 2컬럼 레이아웃(사이드바 필터 + 메인 피드) 조합 완료.
- [x] 루트 경로(`src/app/page.tsx`)에 메인 뷰 연결 완료.

## 5. 최종 체크리스트 (Validation)
- [x] 다중 계정 및 테마 선택 시 OR 조건으로 데이터가 정상 합산되는가?
- [x] 기간 필터의 기본값이 '오늘'로 정상 동작하는가?
- [x] 필터 모두 해제 시 "데이터가 없습니다" 안내 문구가 노출되는가?
- [x] 전역 다크모드 및 반응형 레이아웃이 깨지지 않는가?
